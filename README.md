# URL-Shortener

Это сократитель ссылок. Фронтэнд максимально простой, но при необходимсти можно переписать его под свои нужды. Все ссылки сохраняются в Mongo DB. Перед запуском бэкенд части запускайте его. 

Рекомендуется запускать бэкенд на порту 3000, а фронтэнд на 3001.

## 📋 Используемые технологии

- React 19
- Node.js
- Mongo DB
- TypeScript

## 🚀 Запуск проекта

```npm install``` - устанавливаем все необходимые зависимости

```npm run``` - запускаем фронтэнд часть(в папке frontend-react)

```node index.js``` - запускаем бэкенд-часть(в корневой папке проекта)
